# Claude Certified Developer, Foundations (CCDV-F) - banco de práctica
D1 = "Messages API y fundamentos"
D2 = "Tool use y MCP"
D3 = "Prompt engineering para desarrolladores"
D4 = "Rendimiento y costos"
D5 = "Claude Code y Agent SDK"

EXAM = {
  "code": "CCDV-F",
  "title": "Claude Certified Developer, Foundations",
  "desc": "Simulacro orientado a desarrolladores: Messages API, tool use, MCP, prompt engineering aplicado, optimización de rendimiento/costos y Claude Code / Agent SDK.",
  "file": "02-CCDV-F-Developer.html",
  "domains": [D1, D2, D3, D4, D5],
  "questions": [
# ---------- D1 ----------
(D1, "En la Messages API, ¿dónde se especifica el prompt de sistema?",
 "En el parámetro de nivel superior `system`, no como un mensaje dentro de `messages`",
 ["Como un mensaje con `role: \"system\"` dentro de `messages`", "En el header `anthropic-system`", "En el parámetro `metadata`"],
 "La Messages API solo acepta roles `user` y `assistant` en `messages`; el prompt de sistema va en `system`."),
(D1, "¿Qué parámetro es obligatorio en toda petición a `POST /v1/messages` además de `model` y `messages`?",
 "`max_tokens`",
 ["`temperature`", "`system`", "`stop_sequences`"],
 "`max_tokens` define el máximo de tokens a generar y es obligatorio."),
(D1, "¿Qué headers de autenticación y versión requiere la API directa de Anthropic?",
 "`x-api-key` y `anthropic-version`",
 ["`Authorization: Basic` y `api-version`", "`x-openai-key` y `version`", "Solo `Content-Type`"],
 "La API usa `x-api-key` para autenticar y `anthropic-version` (p. ej. `2023-06-01`) para fijar la versión."),
(D1, "Una respuesta llega con `stop_reason: \"max_tokens\"`. ¿Qué significa?",
 "La generación se cortó al alcanzar el límite de `max_tokens`; la respuesta puede estar incompleta",
 ["El modelo terminó naturalmente su respuesta", "El modelo quiere usar una herramienta", "Se superó el límite de peticiones por minuto"],
 "`end_turn` indica fin natural; `max_tokens` indica truncamiento; `tool_use` indica que el modelo solicita una herramienta."),
(D1, "¿Cómo se mantiene una conversación de varios turnos con la Messages API?",
 "Enviando en cada petición el historial completo de mensajes alternando `user` y `assistant`",
 ["Enviando un `conversation_id` que el servidor recuerda", "La API guarda el estado automáticamente por API key", "Usando el header `keep-alive`"],
 "La Messages API es sin estado: el cliente reenvía el historial relevante en cada llamada."),
(D1, "¿Qué código HTTP indica que se superó un rate limit y cómo debe manejarse?",
 "429; reintentar con backoff exponencial respetando el header `retry-after`",
 ["500; no reintentar nunca", "404; cambiar de modelo", "401; regenerar la API key"],
 "429 = rate limit. Los SDK oficiales reintentan automáticamente con backoff; también existe 529 para sobrecarga."),
(D1, "¿Cómo se envía una imagen a Claude en la Messages API?",
 "Como un bloque de contenido `type: \"image\"` con `source` en base64 o URL dentro del `content` de un mensaje `user`",
 ["Como un archivo adjunto multipart independiente del JSON", "En el parámetro `system`", "Solo mediante la consola web"],
 "Los mensajes admiten arrays de bloques de contenido (text, image, document, etc.)."),
(D1, "Al hacer streaming (`stream: true`), ¿qué tipo de evento contiene los fragmentos incrementales de texto?",
 "`content_block_delta`",
 ["`message_start`", "`ping`", "`message_stop`"],
 "El flujo SSE incluye message_start, content_block_start, content_block_delta (con los deltas), content_block_stop, message_delta y message_stop."),
(D1, "¿Cuál es la ventaja principal del streaming para una interfaz de chat?",
 "Reduce la latencia percibida mostrando texto a medida que se genera",
 ["Reduce el costo por token a la mitad", "Aumenta la ventana de contexto", "Evita la necesidad de `max_tokens`"],
 "El streaming mejora la experiencia (tiempo al primer token) y evita timeouts en respuestas largas; no cambia el precio."),
(D1, "¿Qué contiene el objeto `usage` de una respuesta?",
 "Conteos de tokens de entrada y salida (y de lectura/escritura de caché cuando aplica)",
 ["El costo en dólares de la petición", "El número de usuarios activos", "La latencia de red"],
 "`usage` reporta `input_tokens`, `output_tokens` y, con caché, `cache_creation_input_tokens` y `cache_read_input_tokens`."),
# ---------- D2 ----------
(D2, "¿Qué elementos define una herramienta en el parámetro `tools`?",
 "`name`, `description` e `input_schema` (JSON Schema)",
 ["`url`, `method` y `headers`", "Solo `name`", "`function_code` con el código fuente"],
 "Claude recibe el nombre, una descripción detallada y el esquema de entrada; la ejecución ocurre en tu código (herramientas cliente)."),
(D2, "Claude responde con `stop_reason: \"tool_use\"`. ¿Qué debe hacer tu aplicación?",
 "Ejecutar la herramienta y enviar un nuevo mensaje `user` con un bloque `tool_result` que referencie el `tool_use_id`",
 ["Reenviar el mismo prompt sin cambios", "Enviar el resultado como mensaje `assistant`", "Terminar la conversación"],
 "El ciclo agéntico: tool_use → ejecutar → tool_result (en un turno user) → Claude continúa."),
(D2, "¿Qué factor influye MÁS en que Claude use correctamente una herramienta?",
 "Una descripción clara y detallada: qué hace, cuándo usarla, qué significa cada parámetro y sus limitaciones",
 ["Un nombre corto de dos letras", "Poner la herramienta primero en la lista", "Usar mayúsculas en el nombre"],
 "Las descripciones son el principal insumo del modelo para decidir cuándo y cómo invocar una herramienta."),
(D2, "¿Qué hace `tool_choice: {\"type\": \"any\"}`?",
 "Obliga a Claude a usar alguna de las herramientas disponibles, sin especificar cuál",
 ["Permite que Claude decida si usa herramientas o no", "Prohíbe el uso de herramientas", "Fuerza una herramienta concreta por nombre"],
 "`auto` = decide el modelo; `any` = debe usar alguna; `tool` = una específica; `none` = ninguna."),
(D2, "Una herramienta falla por un timeout. ¿Cómo se comunica a Claude?",
 "Enviando el `tool_result` con `is_error: true` y un mensaje descriptivo del error",
 ["No enviando nada y esperando", "Enviando un mensaje `assistant` con el error", "Reiniciando la conversación sin el historial"],
 "Con `is_error` Claude puede reintentar, usar otra estrategia o informar al usuario."),
(D2, "¿Qué es el Model Context Protocol (MCP)?",
 "Un protocolo abierto para conectar aplicaciones de IA con herramientas y fuentes de datos externas de forma estandarizada",
 ["Un formato de compresión de prompts", "Un modelo de lenguaje de Anthropic", "Un servicio de hosting de modelos"],
 "MCP estandariza cómo un cliente (host) descubre y usa tools, resources y prompts expuestos por servidores."),
(D2, "¿Cuáles son las tres primitivas principales que expone un servidor MCP?",
 "Tools, Resources y Prompts",
 ["Models, Datasets y Metrics", "Users, Roles y Permissions", "Queues, Topics y Streams"],
 "Tools = acciones invocables por el modelo; Resources = datos de contexto; Prompts = plantillas reutilizables."),
(D2, "¿Qué transportes estándar define MCP para la comunicación cliente-servidor?",
 "stdio (procesos locales) y Streamable HTTP (servidores remotos)",
 ["FTP y SMTP", "gRPC exclusivamente", "WebRTC y Bluetooth"],
 "stdio se usa para servidores locales lanzados como subprocesos; Streamable HTTP para remotos (reemplazó a HTTP+SSE)."),
(D2, "¿Sobre qué formato de mensajes se basa MCP?",
 "JSON-RPC 2.0",
 ["SOAP/XML", "Protocol Buffers exclusivamente", "CSV"],
 "Todas las solicitudes, respuestas y notificaciones MCP son mensajes JSON-RPC 2.0."),
(D2, "Claude puede pedir varias herramientas independientes en una sola respuesta. ¿Cómo devuelves los resultados?",
 "En un único mensaje `user` que contiene un bloque `tool_result` por cada `tool_use_id`",
 ["Un mensaje `user` separado por cada resultado, intercalando mensajes vacíos del asistente", "Solo el resultado de la primera herramienta", "En el prompt de sistema"],
 "Con llamadas en paralelo, todos los tool_result van juntos en el siguiente turno de usuario."),
# ---------- D3 ----------
(D3, "Necesitas que Claude devuelva JSON válido que cumpla un esquema para procesarlo automáticamente. ¿Cuál es el enfoque más robusto?",
 "Usar structured outputs / tool use estricto con un JSON Schema definido y validar la salida en código",
 ["Pedir «responde en JSON por favor» y confiar en el resultado", "Subir la temperatura", "Pedir JSON dentro de un bloque de texto libre largo"],
 "Los mecanismos de salida estructurada garantizan conformidad con el esquema; validar en código sigue siendo buena práctica."),
(D3, "¿Por qué se recomienda usar etiquetas XML (p. ej. `<documento>`) en prompts complejos?",
 "Separan claramente instrucciones, contexto y ejemplos, y facilitan extraer partes de la respuesta",
 ["Porque la API rechaza texto sin XML", "Porque reducen el precio por token", "Porque activan un modelo distinto"],
 "Claude fue entrenado para aprovechar estructura; las etiquetas reducen ambigüedad."),
(D3, "Con documentos largos (decenas de miles de tokens), ¿dónde conviene colocar la pregunta?",
 "Al final del prompt, después de los documentos",
 ["Al principio y nunca repetirla", "Dentro del primer documento", "En el header HTTP"],
 "Poner los documentos arriba y la consulta al final suele mejorar la calidad de respuesta en contextos largos."),
(D3, "¿Qué es el «extended thinking»?",
 "Una función que permite al modelo razonar internamente antes de responder, con un presupuesto de tokens configurable",
 ["Un modo que amplía la ventana de contexto a infinito", "Un endpoint para búsqueda web", "Una técnica de caché"],
 "Con extended thinking el modelo genera bloques de razonamiento que mejoran tareas complejas; el razonamiento consume tokens de salida."),
(D3, "¿Qué parámetro controla la aleatoriedad del muestreo y qué valor favorece respuestas más deterministas?",
 "`temperature`; valores bajos (cercanos a 0)",
 ["`max_tokens`; valores altos", "`top_k`; valores negativos", "`stream`; `false`"],
 "Temperaturas bajas son adecuadas para tareas analíticas o de clasificación; más altas para creatividad."),
(D3, "Un clasificador con Claude da resultados inconsistentes entre casos parecidos. ¿Qué mejora es más efectiva?",
 "Añadir ejemplos few-shot representativos, incluyendo casos límite, con la etiqueta correcta",
 ["Eliminar el prompt de sistema", "Aumentar `max_tokens` a 64 000", "Cambiar a streaming"],
 "Ejemplos diversos y bien etiquetados definen la frontera de decisión con mayor precisión."),
(D3, "¿Cuál es la mejor forma de saber si un cambio de prompt mejora realmente tu aplicación?",
 "Ejecutar una evaluación (eval) con un conjunto de casos de prueba y métricas antes y después del cambio",
 ["Probar un par de ejemplos manualmente", "Preguntar a Claude si el prompt es mejor", "Medir la longitud del prompt"],
 "Las evals sistemáticas evitan regresiones y cuantifican mejoras."),
(D3, "¿Qué es «LLM-as-a-judge»?",
 "Usar un modelo para calificar salidas según una rúbrica cuando la evaluación exacta por código no es viable",
 ["Un modelo que dicta sentencias legales", "Un endpoint de moderación", "Una métrica de latencia"],
 "Es útil para criterios subjetivos (tono, completitud) con rúbricas claras; conviene calibrarlo con revisión humana."),
(D3, "Tu prompt de sistema dice «NUNCA JAMÁS uses listas» y el modelo se vuelve demasiado rígido. ¿Qué ajuste es mejor con modelos recientes?",
 "Formular la instrucción en positivo y con su motivo, p. ej. «Escribe en párrafos fluidos porque se leerá en voz alta»",
 ["Añadir más mayúsculas y signos de exclamación", "Repetir la instrucción diez veces", "Eliminar todo el prompt de sistema"],
 "Los modelos actuales siguen instrucciones con precisión; el énfasis excesivo puede provocar sobrecorrección. Explicar el porqué funciona mejor."),
(D3, "Para una tarea compleja (extraer → analizar → redactar), ¿qué patrón ofrece mejor control y trazabilidad?",
 "Prompt chaining: dividir en llamadas sucesivas donde la salida de una alimenta a la siguiente",
 ["Una sola llamada gigante con todas las instrucciones mezcladas", "Llamar tres veces al mismo prompt y elegir al azar", "Usar solo el prompt de sistema"],
 "Encadenar permite validar cada paso, depurar y optimizar individualmente."),
# ---------- D4 ----------
(D4, "¿Qué hace el prompt caching?",
 "Reutiliza prefijos de prompt ya procesados, reduciendo latencia y costo en llamadas que repiten ese prefijo",
 ["Guarda las respuestas para devolverlas sin llamar al modelo", "Comprime el texto del usuario", "Almacena la conversación en el servidor para no reenviarla"],
 "Se marca un punto con `cache_control`; el prefijo hasta ahí se cachea y las lecturas cuestan una fracción del precio normal."),
(D4, "¿Cómo se marca el final del bloque a cachear con prompt caching?",
 "Añadiendo `cache_control: {\"type\": \"ephemeral\"}` al bloque de contenido correspondiente",
 ["Con el header `x-cache: true`", "Con el parámetro `cache: \"forever\"`", "Poniendo el texto entre etiquetas `<cache>`"],
 "El caché se aplica al prefijo (tools, system, messages) hasta el bloque marcado."),
(D4, "¿Cuál es el TTL por defecto del prompt caching?",
 "5 minutos (renovable con cada uso); existe opción de 1 hora con un costo de escritura mayor",
 ["24 horas fijas", "7 días", "No expira nunca"],
 "El caché «ephemeral» dura 5 minutos por defecto y se refresca al usarse; la opción de 1 hora cuesta más al escribir."),
(D4, "Para maximizar aciertos de caché, ¿cómo se debe ordenar el prompt?",
 "Contenido estable primero (herramientas, sistema, documentos fijos) y contenido variable al final",
 ["Contenido variable al principio", "Orden aleatorio en cada llamada", "Poner la fecha y hora actual al inicio del prompt de sistema"],
 "El caché requiere prefijos idénticos; cualquier cambio al principio invalida todo lo posterior."),
(D4, "Debes procesar 100 000 documentos sin necesidad de respuesta inmediata. ¿Qué opción reduce más el costo?",
 "La Message Batches API, con ~50 % de descuento y procesamiento asíncrono (normalmente en menos de 24 h)",
 ["Streaming con temperatura 0", "Llamadas síncronas en paralelo sin límite", "Usar extended thinking con presupuesto máximo"],
 "Batches es ideal para cargas no urgentes y de gran volumen."),
(D4, "Una tarea de clasificación simple de alto volumen y baja latencia: ¿qué modelo suele ser la mejor elección inicial?",
 "Un modelo de la gama Haiku, validando la calidad con una eval",
 ["Siempre el modelo Opus más grande", "Cualquier modelo con extended thinking al máximo", "Un modelo retirado por ser más barato"],
 "Elegir el modelo más pequeño que cumpla el umbral de calidad optimiza costo y latencia."),
(D4, "¿Qué endpoint permite estimar el número de tokens de una petición antes de enviarla?",
 "El endpoint de conteo de tokens (`/v1/messages/count_tokens`)",
 ["`/v1/usage/estimate`", "`/v1/models/price`", "No existe forma de estimarlo"],
 "Contar tokens previamente ayuda a controlar costos y respetar límites de contexto."),
(D4, "Una aplicación recibe con frecuencia errores 529. ¿Qué indica y qué hacer?",
 "La API está temporalmente sobrecargada; reintentar con backoff exponencial y jitter",
 ["La API key es inválida; regenerarla", "El prompt contiene contenido prohibido", "El modelo fue retirado permanentemente"],
 "529 = overloaded. Es transitorio; los SDK manejan reintentos."),
(D4, "¿Cuál de estas acciones NO reduce el costo por petición?",
 "Activar `stream: true`",
 ["Usar prompt caching para prefijos repetidos", "Elegir un modelo más pequeño adecuado a la tarea", "Reducir contexto innecesario enviado en cada llamada"],
 "El streaming cambia cómo se entrega la respuesta, no los tokens facturados."),
(D4, "¿Cómo afectan los tokens de «thinking» al costo cuando se usa extended thinking?",
 "Se facturan como tokens de salida",
 ["Son gratuitos", "Se facturan como tokens de entrada cacheados", "Solo se cobran si la respuesta es correcta"],
 "El razonamiento consume tokens de salida; conviene ajustar el presupuesto a la complejidad."),
# ---------- D5 ----------
(D5, "¿Qué archivo usa Claude Code para cargar instrucciones y contexto persistente de un proyecto?",
 "`CLAUDE.md`",
 ["`README.claude`", "`.clauderc.yaml`", "`package.json`"],
 "CLAUDE.md (en la raíz del repo, en subdirectorios o en `~/.claude/`) se carga automáticamente como memoria del proyecto."),
(D5, "¿Qué comando de Claude Code genera un `CLAUDE.md` inicial analizando el repositorio?",
 "`/init`",
 ["`/start`", "`/setup-md`", "`/new`"],
 "`/init` explora el código y crea un CLAUDE.md con comandos, arquitectura y convenciones."),
(D5, "¿Cómo se ejecuta Claude Code de forma no interactiva, por ejemplo en un pipeline de CI?",
 "Con el modo headless: `claude -p \"<prompt>\"` (opcionalmente con `--output-format json`)",
 ["No es posible fuera de la terminal interactiva", "Con `claude --gui`", "Solo mediante la app de escritorio"],
 "El flag `-p` (print) ejecuta una consulta y termina, ideal para scripts y CI."),
(D5, "¿Qué son los «hooks» en Claude Code?",
 "Comandos que se ejecutan automáticamente en eventos del ciclo de vida (p. ej. `PreToolUse`, `PostToolUse`) configurados en settings",
 ["Plugins del navegador", "Atajos de teclado", "Ramas de git creadas por Claude"],
 "Los hooks permiten aplicar reglas deterministas: formatear tras editar, bloquear comandos peligrosos, notificar, etc."),
(D5, "¿Dónde se configuran los servidores MCP a nivel de proyecto para que todo el equipo los comparta en Claude Code?",
 "En un archivo `.mcp.json` en la raíz del repositorio",
 ["En `CLAUDE.md` exclusivamente", "En `~/.bashrc`", "En el `.gitignore`"],
 "El alcance de proyecto usa `.mcp.json` versionado; también existen alcances local y de usuario."),
(D5, "¿Qué son los subagentes en Claude Code?",
 "Agentes especializados con su propio prompt, herramientas y ventana de contexto, definidos p. ej. en `.claude/agents/`",
 ["Otros usuarios conectados al mismo repositorio", "Versiones antiguas del modelo", "Procesos de git en segundo plano"],
 "Los subagentes aíslan contexto y permiten delegar tareas (revisión, búsqueda, pruebas) sin saturar la conversación principal."),
(D5, "La conversación en Claude Code se volvió muy larga. ¿Qué comando resume el historial para liberar contexto sin perder lo esencial?",
 "`/compact`",
 ["`/clear`", "`/exit`", "`/reset-model`"],
 "`/compact` resume la conversación; `/clear` la borra por completo."),
(D5, "¿Qué es el Claude Agent SDK?",
 "Un SDK que expone el mismo motor agéntico de Claude Code (bucle de agente, herramientas, contexto) para construir agentes propios",
 ["Un SDK para entrenar modelos desde cero", "Una librería de interfaz gráfica", "Un cliente de base de datos"],
 "Permite crear agentes con herramientas integradas, MCP, permisos y gestión de contexto en Python o TypeScript."),
(D5, "Antes de un cambio grande en el código, quieres que Claude Code investigue y proponga un plan sin modificar archivos. ¿Qué usas?",
 "Plan mode (modo planificación)",
 ["El modo de bypass de permisos", "`/compact`", "Un hook PostToolUse"],
 "En plan mode Claude analiza y propone un plan de solo lectura para que lo apruebes antes de ejecutar."),
(D5, "¿Qué ventaja tienen los «slash commands» personalizados en Claude Code?",
 "Reutilizar prompts frecuentes del equipo como comandos (p. ej. definidos como skills o en `.claude/commands/`) versionados con el repo",
 ["Aumentan la ventana de contexto", "Desactivan los permisos", "Cambian el modelo base a uno más barato"],
 "Estandarizan flujos repetitivos (revisión de PR, generación de tests) para todo el equipo."),
]}
